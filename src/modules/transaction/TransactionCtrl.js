'use strict';

angular.module('lostcoin-web').controller('TransactionCtrl', ['$rootScope', '$scope', function ($rootScope, $scope) {

        /* --- MODELS --- */

        /* --- FUNCTIONS --- */

        /* --- RUN --- */

    }]);